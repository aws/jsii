#!/bin/bash
set -euo pipefail

mkdir -p dist/java
mkdir -p maven-repo

(
  cd project
  mvn -B deploy -D altDeploymentRepository=local::default::file://${PWD}/../maven-repo
)

rsync -av maven-repo/ dist/java/

